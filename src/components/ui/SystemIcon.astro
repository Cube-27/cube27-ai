---
import {
  Activity,
  Bot,
  BrainCircuit,
  ClipboardCheck,
  Database,
  Gauge,
  MessagesSquare,
  SearchCheck,
  ShieldCheck,
} from "@lucide/astro";

interface Props {
  name: string;
  size?: number;
}

const { name, size = 18 } = Astro.props;
const icons = {
  activity: Activity,
  bot: Bot,
  brain: BrainCircuit,
  clipboard: ClipboardCheck,
  database: Database,
  gauge: Gauge,
  messages: MessagesSquare,
  search: SearchCheck,
  shield: ShieldCheck,
} as const;
const Icon = icons[name as keyof typeof icons] ?? Activity;
---

<Icon size={size} stroke-width={1.65} aria-hidden="true" />
